#!/usr/bin/env python3
"""Regenerate js/app.js from _app_zh.json. ASCII-only; UTF-8 output."""
import json
from pathlib import Path

ROOT = Path(__file__).resolve().parent
T = json.loads((ROOT / "_app_zh.json").read_text(encoding="utf-8"))


def q(key: str) -> str:
    return json.dumps(T[key], ensure_ascii=False)


def main() -> None:
    o: list[str] = []
    W = o.append

    def block_pick_relation() -> None:
        W("  function pickRelation(userNorm, persona, cos) {")
        W("    const s = sim01(cos);")
        W("    let rel = relationFromBands(s);")
        W('    let reasonKey = "similarity_band";')
        W("    const u = userNorm;")
        W("    const p = persona.vector;")
        W("")
        W("    const ds = (u.structure ?? 0) - (p.structure ?? 0);")
        W("    const dSoc = Math.abs((u.social ?? 0) - (p.social ?? 0));")
        W("    const dStr = Math.abs((u.structure ?? 0) - (p.structure ?? 0));")
        W("    const dEmo = Math.abs((u.emotion_expr ?? 0) - (p.emotion_expr ?? 0));")
        W("    const cU = u.conflict_direct ?? 0;")
        W("    const cP = p.conflict_direct ?? 0;")
        W("")
        W("    const mentorish =")
        W("      (p.structure ?? 0) - (u.structure ?? 0) >= 0.42 &&")
        W("      (p.authority ?? 0) - (u.authority ?? 0) >= 0.28 &&")
        W("      s >= 0.34 &&")
        W("      s <= 0.8;")
        W("")
        W("    const studentish =")
        W("      (u.structure ?? 0) - (p.structure ?? 0) >= 0.4 &&")
        W("      (u.risk ?? 0) - (p.risk ?? 0) >= 0.18 &&")
        W("      s >= 0.34 &&")
        W("      s <= 0.78;")
        W("")
        W("    const partyBuddy =")
        W("      s >= 0.46 &&")
        W("      dSoc < 0.28 &&")
        W("      dStr > 0.42 &&")
        W("      persona.archetypeHints &&")
        W("      persona.archetypeHints.partyEnergy;")
        W("")
        W("    const workAlly =")
        W("      s >= 0.5 &&")
        W("      s <= 0.74 &&")
        W("      dStr < 0.24 &&")
        W("      dEmo > 0.38;")
        W("")
        W("    const rivalish =")
        W("      s >= 0.44 &&")
        W("      s <= 0.7 &&")
        W("      cU * cP < 0 &&")
        W("      Math.abs(cU - cP) >= 0.85 &&")
        W("      persona.archetypeHints &&")
        W("      (persona.archetypeHints.rivalBias || persona.archetypeHints.rebel);")
        W("")
        W("    const strongOppose = cos <= -0.08 || s < 0.26;")
        W("")
        W("    const byId = (id) => quiz.relationshipCatalog.find((r) => r.id === id);")
        W("")
        W("    if (strongOppose) {")
        W('      rel = byId("nemesis");')
        W('      reasonKey = "nemesis_vector_clash";')
        W("    } else if (mentorish && persona.archetypeHints && persona.archetypeHints.mentorBias) {")
        W('      rel = byId("shitu");')
        W('      reasonKey = "shitu_mentor_bias";')
        W("    } else if (mentorish && s < 0.72 && ds < -0.25) {")
        W('      rel = byId("shitu");')
        W('      reasonKey = "shitu_structure_gap";')
        W("    } else if (studentish) {")
        W('      rel = byId("ally");')
        W('      reasonKey = "ally_studentish";')
        W("    } else if (partyBuddy) {")
        W('      rel = byId("party_mate");')
        W('      reasonKey = "party_mate_energy";')
        W("    } else if (workAlly) {")
        W('      rel = byId("plastic_work");')
        W('      reasonKey = "plastic_work_task";')
        W("    } else if (rivalish) {")
        W('      rel = byId("fake_bros");')
        W('      reasonKey = "fake_bros_rival";')
        W("    }")
        W("")
        W("    if (!strongOppose && s >= 0.82) {")
        W('      rel = byId("soulmate");')
        W('      reasonKey = "soulmate_similarity_peak";')
        W("    }")
        W("")
        W("    return {")
        W("      relation: rel,")
        W("      cos,")
        W("      sim01: s,")
        W("      reasonKey,")
        W("      debug: {},")
        W("    };")
        W("  }")

    W("(function () {")
    W('  "use strict";')
    W("")
    W("  const quiz = window.__QUIZ__;")
    W("  const personas = window.__PERSONAS__ || [];")
    W("  const P1_MID = " + q("p1_mid") + ";")
    W("  const P2_A = " + q("p2_a") + ";")
    W("  const P2_B = " + q("p2_b") + ";")
    W("  const MATCH_PREFIX = " + q("match_prefix") + ";")
    W("  const WHY_DIM_FB = " + q("why_dim_fb") + ";")
    W("  const EGG_META = " + q("eggMeta") + ";")
    for key in (
        "why_nemesis",
        "why_soul",
        "why_shitu_bias",
        "why_shitu_gap",
        "why_ally",
        "why_party",
        "why_plastic",
        "why_fake",
        "why_band_clash",
        "why_band_plain",
    ):
        W(f"  const WHY_TPL_{key.upper()} = " + json.dumps(T[key], ensure_ascii=False) + ";")
    W("  const SIM_TONE_VARS = " + json.dumps(T["sim_tone_vars"], ensure_ascii=False) + ";")
    W(
        "  const P2_FALLBACK_BY_REL = "
        + json.dumps(T.get("p2_fallback_by_rel", {}), ensure_ascii=False)
        + ";"
    )
    W("  const P2_FALLBACK_DEFAULT = " + json.dumps(T["p2fallback"], ensure_ascii=False) + ";")
    W("  const STRICT_FILL_POOL = " + json.dumps(T["strict_fill_pool"], ensure_ascii=False) + ";")
    W("  const NGRAM_LEVELS = [5, 6];")
    W("")
    W("  const DIM_IDS = quiz.dimensions.map((d) => d.id);")
    W("")
    W("  const DIM_PHRASE = {")
    for dim in ("social", "structure", "conflict_direct", "emotion_expr", "risk", "authority"):
        if dim == "social":
            keys = ("d_social_u", "d_social_p", "d_social_c")
        elif dim == "structure":
            keys = ("d_struct_u", "d_struct_p", "d_struct_c")
        elif dim == "conflict_direct":
            keys = ("d_conf_u", "d_conf_p", "d_conf_c")
        elif dim == "emotion_expr":
            keys = ("d_emo_u", "d_emo_p", "d_emo_c")
        elif dim == "risk":
            keys = ("d_risk_u", "d_risk_p", "d_risk_c")
        else:
            keys = ("d_auth_u", "d_auth_p", "d_auth_c")
        W(f"    {dim}: {{")
        W(f"      uHigh: {q(keys[0])},")
        W(f"      pHigh: {q(keys[1])},")
        W(f"      close: {q(keys[2])},")
        W("    },")
    W("  };")
    W("")
    W("  function maxAbs(n) {")
    W("    return Math.max(Math.abs(n.yes), Math.abs(n.no), Math.abs(n.uncertain || 0));")
    W("  }")
    W("")
    W("  function computeMaxRawPerDim() {")
    W("    const maxRaw = {};")
    W("    DIM_IDS.forEach((id) => (maxRaw[id] = 0));")
    W("    quiz.questions.forEach((q) => {")
    W("      DIM_IDS.forEach((dim) => {")
    W("        const w = q.weights && q.weights[dim];")
    W("        if (w) maxRaw[dim] += maxAbs(w);")
    W("      });")
    W("    });")
    W("    return maxRaw;")
    W("  }")
    W("")
    W("  const MAX_RAW = computeMaxRawPerDim();")
    W("")
    W("  function emptyVector() {")
    W("    const v = {};")
    W("    DIM_IDS.forEach((id) => (v[id] = 0));")
    W("    return v;")
    W("  }")
    W("")
    W("  function aggregateUserVector(answersByQuestionId) {")
    W("    const raw = emptyVector();")
    W("    quiz.questions.forEach((q) => {")
    W("      const key = answersByQuestionId[q.id];")
    W("      if (!key) return;")
    W("      DIM_IDS.forEach((dim) => {")
    W("        const w = q.weights && q.weights[dim];")
    W("        if (!w) return;")
    W("        const add = w[key];")
    W('        if (typeof add === "number") raw[dim] += add;')
    W("      });")
    W("    });")
    W("    const norm = {};")
    W("    DIM_IDS.forEach((dim) => {")
    W("      const m = MAX_RAW[dim] || 1;")
    W("      norm[dim] = Math.max(-1, Math.min(1, raw[dim] / m));")
    W("    });")
    W("    return { raw, norm };")
    W("  }")
    W("")
    W("  function vectorToArray(v) {")
    W("    return DIM_IDS.map((id) => v[id] || 0);")
    W("  }")
    W("")
    W("  function l2(a) {")
    W("    return Math.sqrt(a.reduce((s, x) => s + x * x, 0));")
    W("  }")
    W("")
    W("  function dot(a, b) {")
    W("    let s = 0;")
    W("    for (let i = 0; i < a.length; i++) s += a[i] * b[i];")
    W("    return s;")
    W("  }")
    W("")
    W("  function cosineSimilarity(u, p) {")
    W("    const ua = vectorToArray(u);")
    W("    const pa = vectorToArray(p);")
    W("    const lu = l2(ua);")
    W("    const lp = l2(pa);")
    W("    if (lu < 1e-6 || lp < 1e-6) return 0;")
    W("    return dot(ua, pa) / (lu * lp);")
    W("  }")
    W("")
    W("  function sim01(cos) {")
    W("    return (cos + 1) / 2;")
    W("  }")
    W("")
    W("  function relationFromBands(s01) {")
    W("    const bands = quiz.relationshipBands;")
    W("    for (let i = 0; i < bands.length; i++) {")
    W("      if (s01 >= bands[i].minSim) {")
    W("        const id = bands[i].relationId;")
    W("        return quiz.relationshipCatalog.find((r) => r.id === id) || quiz.relationshipCatalog[0];")
    W("      }")
    W("    }")
    W('    return quiz.relationshipCatalog.find((r) => r.id === "like_only");')
    W("  }")
    W("")
    block_pick_relation()
    W("")
    W("  function relationCandidatesBySimilarity(s01) {")
    W("    const seen = new Set();")
    W("    const ids = [];")
    W("    for (let i = 0; i < quiz.relationshipBands.length; i++) {")
    W("      const b = quiz.relationshipBands[i];")
    W("      if (s01 >= b.minSim && !seen.has(b.relationId)) {")
    W("        ids.push(b.relationId);")
    W("        seen.add(b.relationId);")
    W("      }")
    W("    }")
    W("    for (let i = 0; i < quiz.relationshipCatalog.length; i++) {")
    W("      const id = quiz.relationshipCatalog[i].id;")
    W("      if (!seen.has(id)) {")
    W("        ids.push(id);")
    W("        seen.add(id);")
    W("      }")
    W("    }")
    W("    return ids;")
    W("  }")
    W("")
    W("  function resolveUniqueResultRows(norm, rawRows) {")
    W("    const byId = (id) => quiz.relationshipCatalog.find((r) => r.id === id) || quiz.relationshipCatalog[0];")
    W("    const groups = new Map();")
    W("    rawRows.forEach((item, idx) => {")
    W("      const id = item.relation.id;")
    W("      if (!groups.has(id)) groups.set(id, []);")
    W("      groups.get(id).push({ item, idx });")
    W("    });")
    W("")
    W("    const used = new Set();")
    W("    const out = new Array(rawRows.length);")
    W("    const pending = [];")
    W("")
    W("    groups.forEach((list) => {")
    W(
        "      list.sort((a, b) => b.item.s - a.item.s || String(a.item.persona.id || \"\").localeCompare(String(b.item.persona.id || \"\")));"
    )
    W("      const win = list[0];")
    W("      used.add(win.item.relation.id);")
    W("      out[win.idx] = win.item;")
    W("      for (let i = 1; i < list.length; i++) pending.push(list[i]);")
    W("    });")
    W("")
    W(
        "    pending.sort((a, b) => b.item.s - a.item.s || String(a.item.persona.id || \"\").localeCompare(String(b.item.persona.id || \"\")));"
    )
    W("    pending.forEach(({ item, idx }) => {")
    W("      const ids = relationCandidatesBySimilarity(item.s);")
    W('      const pickId = ids.find((id) => !used.has(id)) || "like_only";')
    W("      used.add(pickId);")
    W("      const relation = byId(pickId);")
    W('      const reasonKey = "slot_reassigned";')
    W("      const story = buildRelationStory(norm, item.persona, relation, item.cos, item.s, reasonKey);")
    W("      out[idx] = {")
    W("        persona: item.persona,")
    W("        relation,")
    W("        story,")
    W("        hint: story.extra,")
    W("        s: item.s,")
    W("        imgSrc: item.imgSrc,")
    W("        cos: item.cos,")
    W("      };")
    W("    });")
    W("")
    W("    return out;")
    W("  }")
    W("")
    W("  function hashSeed(str) {")
    W("    let h = 5381;")
    W("    for (let i = 0; i < str.length; i++) {")
    W("      h = (h * 33) ^ str.charCodeAt(i);")
    W("    }")
    W("    return h >>> 0;")
    W("  }")
    W("")
    W("  function escapeXml(t) {")
    W("    return String(t)")
    W('      .replace(/&/g, "&amp;")')
    W('      .replace(/</g, "&lt;")')
    W('      .replace(/>/g, "&gt;")')
    W('      .replace(/"/g, "&quot;");')
    W("  }")
    W("")
    W("  function generatedAvatarUrl(persona) {")
    W('    const seed = hashSeed((persona.id || "") + (persona.name || ""));')
    W("    const h1 = seed % 360;")
    W("    const h2 = (seed * 13 + 47) % 360;")
    W('    const initial = (persona.name && persona.name.trim()[0]) || "?";')
    W("    const svg =")
    W('      `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 120 120">` +')
    W('      `<defs><linearGradient id="g" x1="0" y1="0" x2="1" y2="1">` +')
    W('      `<stop offset="0%" stop-color="hsl(${h1},65%,52%)"/>` +')
    W('      `<stop offset="100%" stop-color="hsl(${h2},55%,38%)"/>` +')
    W("      `</linearGradient></defs>` +")
    W('      `<rect width="120" height="120" rx="26" fill="url(#g)"/>` +')
    W(
        '      `<text x="60" y="74" text-anchor="middle" font-size="44" fill="rgba(255,255,255,0.92)" ` +'
    )
    W(
        '      `font-family="system-ui,-apple-system,PingFang SC,Microsoft YaHei,sans-serif" font-weight="700">` +'
    )
    W("      escapeXml(initial) +")
    W("      `</text></svg>`;")
    W('    return "data:image/svg+xml;charset=utf-8," + encodeURIComponent(svg);')
    W("  }")
    W("")
    W("  function personaAvatarSrc(persona) {")
    W("    if (persona.avatar && String(persona.avatar).trim()) return persona.avatar;")
    W("    return generatedAvatarUrl(persona);")
    W("  }")
    W("")
    W("  function topDimensionDiffs(u, p, n) {")
    W("    const rows = DIM_IDS.map((dim) => {")
    W("      const du = u[dim] ?? 0;")
    W("      const dp = p[dim] ?? 0;")
    W("      const diff = du - dp;")
    W("      return { dim, diff, ad: Math.abs(diff) };")
    W("    }).sort((a, b) => b.ad - a.ad);")
    W("    return rows.slice(0, n);")
    W("  }")
    W("")
    W("  function phraseForDim(dim, diff) {")
    W("    const ph = DIM_PHRASE[dim];")
    W('    if (!ph) return "";')
    W("    if (diff > 0.22) return ph.uHigh;")
    W("    if (diff < -0.22) return ph.pHigh;")
    W("    return ph.close;")
    W("  }")
    W("")
    W("  const RELATION_VOICE = {")
    rv = [
        ("soulmate", "rv_soul_0", "rv_soul_1", "rv_soul_2"),
        ("sworn_bros", "rv_sworn_0", "rv_sworn_1", "rv_sworn_2"),
        ("ally", "rv_ally_0", "rv_ally_1", "rv_ally_2"),
        ("fellow_suffering", "rv_fel_0", "rv_fel_1", "rv_fel_2"),
        ("fake_bros", "rv_fake_0", "rv_fake_1", "rv_fake_2"),
        ("like_only", "rv_like_0", "rv_like_1", "rv_like_2"),
        ("plastic_work", "rv_plas_0", "rv_plas_1", "rv_plas_2"),
        ("party_mate", "rv_party_0", "rv_party_1", "rv_party_2"),
        ("shitu", "rv_shitu_0", "rv_shitu_1", "rv_shitu_2"),
        ("nemesis", "rv_nem_0", "rv_nem_1", "rv_nem_2"),
    ]
    for rid, a, b, c in rv:
        W(f"    {rid}: [")
        W(f"      {q(a)},")
        W(f"      {q(b)},")
        W(f"      {q(c)},")
        W("    ],")
    W("  };")
    W("")
    W("  function relationVoiceLine(relationId, personaId) {")
    W("    const list = RELATION_VOICE[relationId] || RELATION_VOICE.like_only;")
    W('    const i = hashSeed(String(relationId) + String(personaId || "")) % list.length;')
    W("    return list[i];")
    W("  }")
    W("")
    W("  const STORY_OPENERS = [")
    for i in range(8):
        W(f"    {q('op%d' % i)},")
    W("  ];")
    W("")
    W("  function pickStoryOpener(personaId, relationId) {")
    W('    const i = hashSeed(String(personaId || "") + String(relationId || "")) % STORY_OPENERS.length;')
    W("    return STORY_OPENERS[i];")
    W("  }")
    W("")
    W("  function simToneBucket(sv) {")
    W('    if (sv >= 0.82) return "82";')
    W('    if (sv >= 0.68) return "68";')
    W('    if (sv >= 0.54) return "54";')
    W('    if (sv >= 0.42) return "42";')
    W('    if (sv >= 0.3) return "30";')
    W('    return "lo";')
    W("  }")
    W("")
    W("  function pickSimToneSentence(sv, relationId, personaId) {")
    W('    const b = simToneBucket(sv);')
    W("    const list = SIM_TONE_VARS[b] || SIM_TONE_VARS.lo || [];")
    W("    if (!list.length) return \"\";")
    W(
        "    const i = hashSeed(String(relationId || \"\") + String(personaId || \"\") + b) % list.length;"
    )
    W("    return list[i] || \"\";")
    W("  }")
    W("")
    W("  function dimLabel(dimId) {")
    W("    const d = quiz.dimensions.find((x) => x.id === dimId);")
    W("    return d ? d.label : dimId;")
    W("  }")
    W("")
    W("  function dimClashPhrase(userNorm, persona, n) {")
    W("    const u = userNorm;")
    W("    const p = persona.vector;")
    W("    const rows = topDimensionDiffs(u, p, n);")
    W("    return rows")
    W("      .filter(({ ad }) => ad >= 0.28)")
    W("      .map(({ dim, diff }) => {")
    W("        const lab = dimLabel(dim);")
    W(
        "        if (diff > 0) return `"
        + "\u300c${lab}\u300d"
        + json.dumps(T["dim_u_suffix"], ensure_ascii=False)[1:-1]
        + "`;"
    )
    W(
        "        return `"
        + "\u300c${lab}\u300d"
        + json.dumps(T["dim_p_suffix"], ensure_ascii=False)[1:-1]
        + "`;"
    )
    W("      })")
    W(f"      .join({q('dim_join')});")
    W("  }")
    W("")
    W("  function normTxt(t) {")
    W('    return String(t || "").replace(/\\s+/g, " ").trim();')
    W("  }")
    W("")
    W("  function splitSentencesCN(text) {")
    W('    const t = String(text || "").trim();')
    W("    if (!t) return [];")
    W("    const out = [];")
    W("    let buf = \"\";")
    W("    for (let i = 0; i < t.length; i++) {")
    W("      buf += t[i];")
    W("      if (/[\\u3002\\uff01\\uff1f\\uff1b]/.test(t[i])) {")
    W("        const seg = buf.trim();")
    W("        if (seg) out.push(seg);")
    W('        buf = "";')
    W("      }")
    W("    }")
    W("    const tail = buf.trim();")
    W("    if (tail) out.push(tail);")
    W("    return out;")
    W("  }")
    W("")
    W("  function dedupeStoryNarrative(st) {")
    W('    const order = ["why", "p1", "tone", "p2", "p3", "extra"];')
    W("    const seen = new Set();")
    W("    const res = {};")
    W("    for (const k of order) {")
    W("      const raw = st[k];")
    W('      if (raw == null || raw === "") {')
    W('        res[k] = k === "extra" ? null : "";')
    W("        continue;")
    W("      }")
    W("      const sents = splitSentencesCN(raw);")
    W("      const kept = [];")
    W("      for (const s of sents) {")
    W("        const nk = normTxt(s);")
    W("        if (!nk) continue;")
    W("        if (seen.has(nk)) continue;")
    W("        seen.add(nk);")
    W("        kept.push(s);")
    W("      }")
    W("      const joined = kept.join(\"\");")
    W('      res[k] = joined || (k === "extra" ? null : "");')
    W("    }")
    W("    return res;")
    W("  }")
    W("")
    W("  function fillWhyTpl(tpl, o) {")
    W("    return tpl")
    W("      .replace(/__TITLE__/g, o.title)")
    W("      .replace(/__NAME__/g, o.name)")
    W("      .replace(/__PCT__/g, String(o.pct))")
    W("      .replace(/__COS__/g, o.cos)")
    W("      .replace(/__CLASH__/g, o.clash);")
    W("  }")
    W("")
    W("  function buildWhyThisRelation(reasonKey, relation, userNorm, persona, cos, s) {")
    W("    const title = relation.title;")
    W('    const name = persona.name || "\u4ed6";')
    W("    const pct = Math.round(s * 100);")
    W("    const clashRaw = dimClashPhrase(userNorm, persona, 3);")
    W("    const cosStr = cos.toFixed(2);")
    W("    const oBase = { title, name, pct, cos: cosStr };")
    W("    const oWithFb = Object.assign({}, oBase, { clash: clashRaw || WHY_DIM_FB });")
    W("    const oBand = Object.assign({}, oBase, { clash: clashRaw || \"\" });")
    W("    switch (reasonKey) {")
    W('      case "nemesis_vector_clash":')
    W("        return fillWhyTpl(WHY_TPL_WHY_NEMESIS, oWithFb);")
    W('      case "soulmate_similarity_peak":')
    W("        return fillWhyTpl(WHY_TPL_WHY_SOUL, oWithFb);")
    W('      case "shitu_mentor_bias":')
    W("        return fillWhyTpl(WHY_TPL_WHY_SHITU_BIAS, oWithFb);")
    W('      case "shitu_structure_gap":')
    W("        return fillWhyTpl(WHY_TPL_WHY_SHITU_GAP, oWithFb);")
    W('      case "ally_studentish":')
    W("        return fillWhyTpl(WHY_TPL_WHY_ALLY, oWithFb);")
    W('      case "party_mate_energy":')
    W("        return fillWhyTpl(WHY_TPL_WHY_PARTY, oWithFb);")
    W('      case "plastic_work_task":')
    W("        return fillWhyTpl(WHY_TPL_WHY_PLASTIC, oWithFb);")
    W('      case "fake_bros_rival":')
    W("        return fillWhyTpl(WHY_TPL_WHY_FAKE, oWithFb);")
    W('      case "slot_reassigned":')
    W('        return "";')
    W('      case "similarity_band":')
    W(
        "        return fillWhyTpl(clashRaw ? WHY_TPL_WHY_BAND_CLASH : WHY_TPL_WHY_BAND_PLAIN, clashRaw ? oBand : oWithFb);"
    )
    W("      default:")
    W(
        "        return fillWhyTpl(clashRaw ? WHY_TPL_WHY_BAND_CLASH : WHY_TPL_WHY_BAND_PLAIN, clashRaw ? oBand : oWithFb);"
    )
    W("    }")
    W("  }")
    W("")
    W("  function buildRelationStory(userNorm, persona, relation, cos, s, reasonKey) {")
    W("    const u = userNorm;")
    W("    const p = persona.vector;")
    W("    const top = topDimensionDiffs(u, p, 3);")
    W("")
    W("    const why = buildWhyThisRelation(reasonKey, relation, userNorm, persona, cos, s);")
    W("    const p1 = relation.title + P1_MID + relation.blurb;")
    W("    const tone = pickSimToneSentence(s, relation.id, persona.id);")
    W("    const p2fb = P2_FALLBACK_BY_REL[relation.id] || P2_FALLBACK_DEFAULT;")
    W("")
    W("    const dimPartsRaw = top")
    W("      .map(({ dim, diff }) => phraseForDim(dim, diff))")
    W("      .filter(Boolean);")
    W("    const dimSeen = new Set();")
    W("    const dimParts = [];")
    W("    for (let i = 0; i < dimPartsRaw.length; i++) {")
    W("      const ph = dimPartsRaw[i];")
    W("      const nk = normTxt(ph);")
    W("      if (dimSeen.has(nk)) continue;")
    W("      dimSeen.add(nk);")
    W("      dimParts.push(ph);")
    W("    }")
    W("    const opener = pickStoryOpener(persona.id, relation.id);")
    W("    const p2 =")
    W("      dimParts.length > 0")
    W("        ? `${P2_A}${relation.title}${P2_B}${opener}${dimParts.join(\"\")}`.trim()")
    W("        : `${P2_A}${relation.title}${P2_B}${opener}${p2fb}`.trim();")
    W("")
    W("    const p3 = relationVoiceLine(relation.id, persona.id);")
    W("")
    W("    const extra = secondaryHint(userNorm, persona, relation.id, s);")
    W("    const merged = dedupeStoryNarrative({ why, p1, tone, p2, p3, extra });")
    W("    if (!merged.p2) {")
    W("      merged.p2 =")
    W("        dimParts.length > 0")
    W("          ? `${P2_A}${relation.title}${P2_B}${opener}`.trim()")
    W("          : `${P2_A}${relation.title}${P2_B}${opener}${p2fb}`.trim();")
    W("    }")
    W("    return merged;")
    W("  }")
    W("")
    W("  function normGramSource(text) {")
    W('    return String(text || "").replace(/\\s+/g, "").trim();')
    W("  }")
    W("")
    W("  function createGramBank() {")
    W("    const byN = {};")
    W("    for (let i = 0; i < NGRAM_LEVELS.length; i++) {")
    W("      byN[NGRAM_LEVELS[i]] = new Set();")
    W("    }")
    W("    return { byN };")
    W("  }")
    W("")
    W("  function hasGramConflict(text, bank) {")
    W("    const s = normGramSource(text);")
    W("    if (!s) return false;")
    W("    for (let ni = 0; ni < NGRAM_LEVELS.length; ni++) {")
    W("      const n = NGRAM_LEVELS[ni];")
    W("      const set = bank.byN[n];")
    W("      if (s.length < n) continue;")
    W("      for (let i = 0; i <= s.length - n; i++) {")
    W("        if (set.has(s.slice(i, i + n))) return true;")
    W("      }")
    W("    }")
    W("    return false;")
    W("  }")
    W("")
    W("  function commitGrams(text, bank) {")
    W("    const s = normGramSource(text);")
    W("    if (!s) return;")
    W("    for (let ni = 0; ni < NGRAM_LEVELS.length; ni++) {")
    W("      const n = NGRAM_LEVELS[ni];")
    W("      const set = bank.byN[n];")
    W("      if (s.length < n) continue;")
    W("      for (let i = 0; i <= s.length - n; i++) {")
    W("        set.add(s.slice(i, i + n));")
    W("      }")
    W("    }")
    W("  }")
    W("")
    W("  function splitIntoAtomicUnits(text) {")
    W('    const t = String(text || "").trim();')
    W("    if (!t) return [];")
    W("    const sentences = splitSentencesCN(t);")
    W("    const units = [];")
    W("    for (let si = 0; si < sentences.length; si++) {")
    W("      const sent = sentences[si].trim();")
    W("      if (!sent) continue;")
    W("      if (sent.length <= 24) {")
    W("        units.push(sent);")
    W("        continue;")
    W("      }")
    W("      const chunks = sent.split(/[\\u3001\\uff0c\\uff1b\\uff1a]/);")
    W("      if (chunks.length === 1) {")
    W("        units.push(sent);")
    W("        continue;")
    W("      }")
    W("      for (let ci = 0; ci < chunks.length; ci++) {")
    W("        const c = chunks[ci].trim();")
    W("        if (c) units.push(c);")
    W("      }")
    W("    }")
    W("    return units.length ? units : [t];")
    W("  }")
    W("")
    W("  function pickFromStrictPool(salt, bank) {")
    W("    const pool = STRICT_FILL_POOL || [];")
    W("    if (!pool.length) return \"\";")
    W("    const base = hashSeed(String(salt || \"\"));")
    W("    for (let j = 0; j < pool.length; j++) {")
    W("      const line = pool[(base + j) % pool.length];")
    W("      if (line && !hasGramConflict(line, bank)) {")
    W("        commitGrams(line, bank);")
    W("        return line;")
    W("      }")
    W("    }")
    W("    const h = hashSeed(String(salt) + \":strict\");")
    W('    const line = "\u8865\u4f4d\u7f16\u53f7" + String(h % 100000) + "\uff1a\u6b64\u5361\u7247\u72ec\u7528\u8bcd\u5757\uff0c\u4e0d\u4e0e\u5176\u4ed6\u6807\u7b7e\u590d\u7528\u3002";')
    W("    if (!hasGramConflict(line, bank)) {")
    W("      commitGrams(line, bank);")
    W("      return line;")
    W("    }")
    W('    return "";')
    W("  }")
    W("")
    W("  function stripFieldAgainstGrams(raw, bank, salt) {")
    W('    if (raw == null || raw === "") return "";')
    W("    const units = splitIntoAtomicUnits(raw);")
    W("    const kept = [];")
    W("    for (let ui = 0; ui < units.length; ui++) {")
    W("      const u = units[ui];")
    W("      if (!hasGramConflict(u, bank)) {")
    W("        commitGrams(u, bank);")
    W("        kept.push(u);")
    W("      } else {")
    W('        const rep = pickFromStrictPool(salt + ":" + ui, bank);')
    W("        if (rep) kept.push(rep);")
    W("      }")
    W("    }")
    W('    return kept.join("");')
    W("  }")
    W("")
    W("  function applyGlobalStrictNgramDedupe(rows, bank) {")
    W('    const fieldOrder = ["why", "p1", "tone", "p2", "p3", "extra"];')
    W("    for (let ri = 0; ri < rows.length; ri++) {")
    W("      const row = rows[ri];")
    W("      const st = row.story;")
    W("      const rid = row.relation && row.relation.id;")
    W('      const pid = (row.persona && row.persona.id) || "";')
    W("      for (let fi = 0; fi < fieldOrder.length; fi++) {")
    W("        const k = fieldOrder[fi];")
    W("        const raw = st[k];")
    W('        if (raw == null || raw === "") {')
    W('          if (k === "extra") st[k] = null;')
    W('          else st[k] = "";')
    W("          continue;")
    W("        }")
    W('        st[k] = stripFieldAgainstGrams(raw, bank, ri + ":" + rid + ":" + pid + ":" + k);')
    W('        if (k === "extra" && !String(st[k] || "").trim()) st[k] = null;')
    W("      }")
    W("    }")
    W("  }")
    W("")
    W("  function rebuildP2Strict(row, bank) {")
    W("    const rel = row.relation;")
    W("    const persona = row.persona;")
    W("    const opener = pickStoryOpener(persona.id, rel.id);")
    W("    const fb = P2_FALLBACK_BY_REL[rel.id] || P2_FALLBACK_DEFAULT;")
    W("    const cands = [];")
    W("    cands.push(`${P2_A}${rel.title}${P2_B}${opener}${fb}`.trim());")
    W(
        "    cands.push(`${P2_A}${rel.title}${P2_B}${opener}${fb}\uff08\u843d\u5728${persona.name || \"\u8fd9\u4f4d\"}\u8eab\u4e0a\u53c8\u591a\u4e00\u5c42\u4e2a\u4eba\u4e60\u60ef\uff09`.trim());"
    )
    W(
        "    cands.push(`${P2_A}${rel.title}${P2_B}${opener}\u843d\u5728${persona.name || \"\u8fd9\u4f4d\"}\u8fd9\u8fb9\u7684\u7ec6\u8282\u7f1d\u8fdb\u300c${rel.title}\u300d\u91cc\uff0c\u8bfb\u51fa\u6765\u4f1a\u662f\u53e6\u4e00\u6761\u526f\u7ebf\u3002`.trim());"
    )
    W("    for (let i = 0; i < cands.length; i++) {")
    W("      const c = cands[i];")
    W("      if (c && !hasGramConflict(c, bank)) {")
    W("        commitGrams(c, bank);")
    W("        return c;")
    W("      }")
    W("    }")
    W('    const salt = (rel.id || "") + ":" + (persona.id || "") + ":p2fb";')
    W("    return pickFromStrictPool(salt, bank);")
    W("  }")
    W("")
    W("  function refillStoryFieldsStrict(rows, bank) {")
    W('    const fieldOrder = ["why", "tone", "p3", "p2", "p1", "extra"];')
    W("    for (let ri = 0; ri < rows.length; ri++) {")
    W("      const row = rows[ri];")
    W("      const st = row.story;")
    W("      const rid = row.relation && row.relation.id;")
    W('      const pid = (row.persona && row.persona.id) || "";')
    W('      const saltBase = ri + ":" + rid + ":" + pid;')
    W("      for (let fi = 0; fi < fieldOrder.length; fi++) {")
    W("        const k = fieldOrder[fi];")
    W('        if (k === "extra") continue;')
    W("        const cur = st[k];")
    W("        if (cur != null && String(cur).trim()) continue;")
    W('        if (k === "p1") {')
    W("          const rel = row.relation;")
    W("          const baseP1 = rel.title + P1_MID + rel.blurb;")
    W('          let p1 = stripFieldAgainstGrams(baseP1, bank, saltBase + ":p1re");')
    W('          if (!String(p1 || "").trim()) p1 = pickFromStrictPool(saltBase + ":p1", bank);')
    W("          st.p1 = p1;")
    W("          continue;")
    W("        }")
    W('        if (k === "p2") {')
    W("          let p2 = rebuildP2Strict(row, bank);")
    W('          if (!String(p2 || "").trim()) p2 = pickFromStrictPool(saltBase + ":p2x", bank);')
    W("          st.p2 = p2;")
    W("          continue;")
    W("        }")
    W('        st[k] = pickFromStrictPool(saltBase + ":" + k, bank);')
    W("      }")
    W('      if (!String(st.extra || "").trim()) st.extra = null;')
    W("    }")
    W("  }")
    W("")
    W("  function secondaryHint(userNorm, persona, primaryId, s) {")
    W("    const p = persona.vector;")
    W("    const u = userNorm;")
    W('    if (primaryId === "shitu" && Math.abs((u.risk ?? 0) - (p.risk ?? 0)) < 0.2) {')
    W(f"      return {q('sec_shitu')};")
    W("    }")
    W('    if (primaryId === "party_mate" && (u.structure ?? 0) > 0.35) {')
    W(f"      return {q('sec_party')};")
    W("    }")
    W('    if (primaryId === "fake_bros" && s > 0.55) {')
    W(f"      return {q('sec_fake')};")
    W("    }")
    W("    return null;")
    W("  }")
    W("")
    W('  const elIntro = document.getElementById("screen-intro");')
    W('  const elQuiz = document.getElementById("screen-quiz");')
    W('  const elResult = document.getElementById("screen-result");')
    W('  const elStart = document.getElementById("btn-start");')
    W('  const elNext = document.getElementById("btn-next");')
    W('  const elBack = document.getElementById("btn-back");')
    W('  const elProgress = document.getElementById("progress-text");')
    W('  const elQText = document.getElementById("question-text");')
    W('  const elOpts = document.getElementById("options");')
    W('  const elCards = document.getElementById("result-cards");')
    W('  const elWarn = document.getElementById("uncertain-warn");')
    W('  const elDisclaimer = document.getElementById("disclaimer");')
    W("")
    W("  let idx = 0;")
    W("  const answers = {};")
    W("")
    W("  function setScreen(name) {")
    W("    [elIntro, elQuiz, elResult].forEach((n) => n && n.classList.add(\"hidden\"));")
    W('    if (name === "intro") elIntro.classList.remove("hidden");')
    W('    if (name === "quiz") elQuiz.classList.remove("hidden");')
    W('    if (name === "result") elResult.classList.remove("hidden");')
    W("  }")
    W("")
    W("  function countUncertain() {")
    W("    let c = 0;")
    W("    Object.keys(answers).forEach((k) => {")
    W('      if (answers[k] === "uncertain") c++;')
    W("    });")
    W("    return c;")
    W("  }")
    W("")
    W("  function renderQuestion() {")
    W("    const q = quiz.questions[idx];")
    W("    elProgress.textContent = `\u7b2c ${idx + 1} / ${quiz.questions.length} \u9898`;")
    W("    elQText.textContent = q.text;")
    W('    elOpts.innerHTML = "";')
    W("    quiz.optionKeys.forEach((k) => {")
    W('      const b = document.createElement("button");')
    W('      b.type = "button";')
    W('      b.className = "opt-btn";')
    W("      b.dataset.value = k;")
    W("      b.textContent = quiz.optionLabels[k];")
    W("      if (answers[q.id] === k) b.classList.add(\"selected\");")
    W('      b.addEventListener("click", () => {')
    W("        answers[q.id] = k;")
    W('        Array.from(elOpts.querySelectorAll(".opt-btn")).forEach((x) => x.classList.remove("selected"));')
    W('        b.classList.add("selected");')
    W("        elNext.disabled = false;")
    W("      });")
    W("      elOpts.appendChild(b);")
    W("    });")
    W("    elNext.disabled = !answers[q.id];")
    W("    elBack.disabled = idx === 0;")
    W('    elWarn.classList.toggle("hidden", countUncertain() < 8);')
    W("  }")
    W("")
    W("  function escapeHtml(text) {")
    W('    const d = document.createElement("div");')
    W("    d.textContent = text;")
    W("    return d.innerHTML;")
    W("  }")
    W("")
    W("  function allUncertainAnswers() {")
    W("    if (!quiz.questions.length) return false;")
    W('    return quiz.questions.every((q) => answers[q.id] === "uncertain");')
    W("  }")
    W("")
    W("  function shuffleArray(arr) {")
    W("    const a = arr.slice();")
    W("    for (let i = a.length - 1; i > 0; i--) {")
    W("      const j = Math.floor(Math.random() * (i + 1));")
    W("      const tmp = a[i];")
    W("      a[i] = a[j];")
    W("      a[j] = tmp;")
    W("    }")
    W("    return a;")
    W("  }")
    W("")
    W("  function zackeryAvatarUrl() {")
    W('    return generatedAvatarUrl({ id: "zackery_egg", name: "Zackery" });')
    W("  }")
    W("")
    W("  function goResultZackeryEgg() {")
    W('    elCards.innerHTML = "";')
    W('    const accordion = document.createElement("div");')
    W('    accordion.className = "result-accordion result-accordion--egg";')
    W("")
    W("    const titles = quiz.relationshipCatalog.map((r) => r.title);")
    W("    const four = shuffleArray(titles).slice(0, 4);")
    W(f"    const eggLine = {q('eggLine')};")
    W("")
    W("    four.forEach((tagLabel) => {")
    W('      const row = document.createElement("div");')
    W('      row.className = "result-row";')
    W("")
    W('      const tagBtn = document.createElement("button");')
    W('      tagBtn.type = "button";')
    W('      tagBtn.className = "relation-tag-btn";')
    W("      tagBtn.textContent = tagLabel;")
    W('      tagBtn.setAttribute("aria-expanded", "false");')
    W("")
    W('      const detail = document.createElement("div");')
    W('      detail.className = "tag-detail hidden";')
    W("      const img = zackeryAvatarUrl();")
    W("      detail.innerHTML = `")
    W('        <div class="tag-detail-inner tag-detail--egg">')
    W('          <div class="card-head">')
    W(
        '            <img class="avatar" src="${img}" alt="" width="72" height="72" loading="lazy" />'
    )
    W("            <div>")
    W('              <h3 class="card-name">Zackery</h3>')
    W('              <p class="card-meta">${EGG_META}</p>')
    W("            </div>")
    W("          </div>")
    W('          <p class="detail-relation-label">${escapeHtml(tagLabel)}</p>')
    W('          <div class="card-story">')
    W("            <p>${escapeHtml(eggLine)}</p>")
    W("          </div>")
    W("        </div>")
    W("      `;")
    W("")
    W('      tagBtn.addEventListener("click", () => {')
    W('        const wasOpen = !detail.classList.contains("hidden");')
    W('        accordion.querySelectorAll(".tag-detail").forEach((d) => d.classList.add("hidden"));')
    W('        accordion.querySelectorAll(".relation-tag-btn").forEach((b) => {')
    W('          b.setAttribute("aria-expanded", "false");')
    W('          b.classList.remove("is-open");')
    W("        });")
    W("        if (!wasOpen) {")
    W('          detail.classList.remove("hidden");')
    W('          tagBtn.setAttribute("aria-expanded", "true");')
    W('          tagBtn.classList.add("is-open");')
    W("        }")
    W("      });")
    W("")
    W("      row.appendChild(tagBtn);")
    W("      row.appendChild(detail);")
    W("      accordion.appendChild(row);")
    W("    });")
    W("")
    W("    elCards.appendChild(accordion);")
    W(f'    elDisclaimer.textContent = quiz.disclaimer + {q("eggDisclaimer")};')
    W('    setScreen("result");')
    W("  }")
    W("")
    W("  function goResult() {")
    W("    if (allUncertainAnswers()) {")
    W("      goResultZackeryEgg();")
    W("      return;")
    W("    }")
    W("")
    W("    const { norm } = aggregateUserVector(answers);")
    W('    elCards.innerHTML = "";')
    W("")
    W('    const accordion = document.createElement("div");')
    W('    accordion.className = "result-accordion";')
    W("")
    W("    const rawRows = personas.map((persona) => {")
    W("      const cos = cosineSimilarity(norm, persona.vector);")
    W("      const { relation, sim01: s, reasonKey } = pickRelation(norm, persona, cos);")
    W("      const story = buildRelationStory(norm, persona, relation, cos, s, reasonKey);")
    W("      const hint = story.extra;")
    W("      const imgSrc = personaAvatarSrc(persona);")
    W("      return { persona, relation, story, hint, s, imgSrc, cos };")
    W("    });")
    W("")
    W("    const rows = resolveUniqueResultRows(norm, rawRows);")
    W("")
    W("    const catalogOrder = {};")
    W("    quiz.relationshipCatalog.forEach((r, i) => {")
    W("      catalogOrder[r.id] = i;")
    W("    });")
    W("")
    W("    rows.sort((a, b) => (catalogOrder[a.relation.id] ?? 999) - (catalogOrder[b.relation.id] ?? 999));")
    W("    const gramBank = createGramBank();")
    W("    applyGlobalStrictNgramDedupe(rows, gramBank);")
    W("    refillStoryFieldsStrict(rows, gramBank);")
    W("")
    W("    rows.forEach((item) => {")
    W("      const relation = item.relation;")
    W("      const items = [item];")
    W('      const row = document.createElement("div");')
    W('      row.className = "result-row";')
    W("")
    W('      const tagBtn = document.createElement("button");')
    W('      tagBtn.type = "button";')
    W('      tagBtn.className = "relation-tag-btn";')
    W("      tagBtn.textContent = relation.title;")
    W('      tagBtn.setAttribute("aria-expanded", "false");')
    W("")
    W('      const detail = document.createElement("div");')
    W('      detail.className = "tag-detail hidden";')
    W("")
    W("      const blocks = items")
    W("        .map((inner) => {")
    W("          const h = inner.hint ? `<p>${escapeHtml(inner.hint)}</p>` : \"\";")
    W(
        '          const whyP = inner.story.why ? `<p class="why-this-tag">${escapeHtml(inner.story.why)}</p>` : "";'
    )
    W("          return `")
    W('        <section class="persona-result-block">')
    W('          <div class="card-head">')
    W(
        '            <img class="avatar" src="${inner.imgSrc}" alt="" width="72" height="72" loading="lazy" />'
    )
    W("            <div>")
    W(
        '              <h3 class="card-name">${escapeHtml(inner.persona.name || "")}</h3>'
    )
    W(
        '              <p class="card-meta">${MATCH_PREFIX}${Math.round(inner.s * 100)}%</p>'
    )
    W("            </div>")
    W("          </div>")
    W('          <div class="card-story">')
    W("            ${whyP}")
    W('            <p>${escapeHtml(inner.story.p1)}</p>')
    W('            <p class="tone-line">${escapeHtml(inner.story.tone)}</p>')
    W('            <p>${escapeHtml(inner.story.p2)}</p>')
    W(
        "            ${inner.story.p3 ? `<p>${escapeHtml(inner.story.p3)}</p>` : \"\"}"
    )
    W("            ${h}")
    W("          </div>")
    W("        </section>`;")
    W("        })")
    W('        .join("");')
    W("")
    W("      detail.innerHTML = `")
    W('        <div class="tag-detail-inner tag-detail--group">')
    W(
        '          <p class="detail-relation-label">${escapeHtml(relation.title)}</p>'
    )
    W("          ${blocks}")
    W("        </div>")
    W("      `;")
    W("")
    W('      tagBtn.addEventListener("click", () => {')
    W('        const wasOpen = !detail.classList.contains("hidden");')
    W('        accordion.querySelectorAll(".tag-detail").forEach((d) => d.classList.add("hidden"));')
    W('        accordion.querySelectorAll(".relation-tag-btn").forEach((b) => {')
    W('          b.setAttribute("aria-expanded", "false");')
    W('          b.classList.remove("is-open");')
    W("        });")
    W("        if (!wasOpen) {")
    W('          detail.classList.remove("hidden");')
    W('          tagBtn.setAttribute("aria-expanded", "true");')
    W('          tagBtn.classList.add("is-open");')
    W("        }")
    W("      });")
    W("")
    W("      row.appendChild(tagBtn);")
    W("      row.appendChild(detail);")
    W("      accordion.appendChild(row);")
    W("    });")
    W("")
    W("    elCards.appendChild(accordion);")
    W("    elDisclaimer.textContent = quiz.disclaimer;")
    W('    setScreen("result");')
    W("  }")
    W("")
    W('  elStart.addEventListener("click", () => {')
    W("    idx = 0;")
    W("    Object.keys(answers).forEach((k) => delete answers[k]);")
    W('    setScreen("quiz");')
    W("    renderQuestion();")
    W("  });")
    W("")
    W('  elNext.addEventListener("click", () => {')
    W("    const q = quiz.questions[idx];")
    W("    if (!answers[q.id]) return;")
    W("    if (idx < quiz.questions.length - 1) {")
    W("      idx++;")
    W("      renderQuestion();")
    W("    } else {")
    W("      goResult();")
    W("    }")
    W("  });")
    W("")
    W('  elBack.addEventListener("click", () => {')
    W("    if (idx > 0) {")
    W("      idx--;")
    W("      renderQuestion();")
    W("    }")
    W("  });")
    W("")
    W('  document.getElementById("btn-retry").addEventListener("click", () => {')
    W('    setScreen("intro");')
    W("  });")
    W("})();")

    (ROOT / "js/app.js").write_text("\n".join(o) + "\n", encoding="utf-8")
    print("written", len(o), "lines")


if __name__ == "__main__":
    main()
